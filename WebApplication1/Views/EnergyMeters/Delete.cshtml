@model WebApplication1.Entities.EnergyMeter
@{

}

@using (Html.BeginForm("Delete", "EnergyMeters", FormMethod.Post))
{
    
    
    @Html.EditorFor(model => model.SerialId, new { htmlAttributes = new { @class = "form-control", @type = "hidden" } });
    <input type="submit" value="Delete" class="btn btn-primary" id="SubmitBtn" />

    @Html.AntiForgeryToken()
}